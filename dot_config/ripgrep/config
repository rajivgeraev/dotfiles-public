# Опции по умолчанию для rg. Одна опция на строку, кавычки не нужны.
# Подключается через RIPGREP_CONFIG_PATH (см. ~/.config/zsh/conf.d/rg.zsh).

# Регистронезависимый поиск, пока в запросе нет заглавных букв.
--smart-case

# Служебные каталоги git никогда не интересны.
--glob=!.git/
