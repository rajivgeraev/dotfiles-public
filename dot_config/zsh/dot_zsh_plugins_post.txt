# Фаза 2 из 2 — загружается ПОСЛЕ compinit (см. conf.d/antidote-post.zsh).
#
# В отличие от conf.d, порядок строк В ЭТОМ ФАЙЛЕ значим.

# Заменяет меню завершения на fzf. Обязан идти после compinit (иначе не видит
# систему завершений) и раньше всех, кто оборачивает виджеты zle, — то есть
# раньше двух последних строк этого файла.
Aloxaf/fzf-tab

# Автозакрытие кавычек и скобок
hlissner/zsh-autopair
# Заголовок окна терминала по текущей команде
olets/zsh-window-title

# Ниже — плагины, оборачивающие виджеты zle. Только после fzf-tab.
# Подсказки по истории
zsh-users/zsh-autosuggestions
# Подсветка синтаксиса — всегда последней строкой файла
zsh-users/zsh-syntax-highlighting
